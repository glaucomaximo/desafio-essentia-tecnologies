# Rodada de Modernização - 2026-09-02

Autor: Glauco Maximo <glaucomaximo@gmail.com>

## Resumo Executivo

O projeto foi inspecionado, documentado e modernizado de forma incremental. O CRUD de tarefas foi preservado, a API ganhou rota versionada sem remover o alias legado, dependências de runtime foram atualizadas e os controles locais de qualidade passaram. A validação local de Docker continua bloqueada porque o Docker CLI não está instalado ou não está no PATH do host.

## Alterações Implementadas

- Upgrade para Node.js 24.15.0, TypeScript 6, Angular 22 e Express 5.
- Rotas canônicas `/api/v1/tasks` com `/api/tasks` preservado.
- Factory testável da aplicação com injeção de repositório e readiness check.
- Prettier, ESLint, scripts de SBOM e controles de qualidade.
- README, OpenAPI, ADRs, auditoria, plano, histórico de alterações, guia de contribuição, política de segurança e runbook.

## Melhorias de Segurança

- `npm audit --omit=dev` reduziu runtime para 0 vulnerabilidades conhecidas.
- Limite de requisições configurável nas rotas `/api`.
- `JSON_BODY_LIMIT` configurável.
- Erros de JSON inválido retornam 400.
- Logs estruturados com redação de chaves sensíveis.
- Varredura de padrões de segredo no CI.
- Containers sem root e sem segredos embutidos.

## Melhorias Arquiteturais

- Monólito modular preservado por ser suficiente ao escopo.
- Limite HTTP/repositório ficou testável por injeção de dependência.
- Health separado em liveness/readiness.
- Decisões registradas em ADR.

## Alterações de Dependências

- Angular 18 -> Angular 22.
- Express 4 -> Express 5.
- TypeScript 5.5 -> TypeScript 6.0.
- Zone.js 0.14 -> 0.16.
- Removido `@angular/animations` não utilizado.
- Adicionado `@cyclonedx/cyclonedx-npm` para SBOM.
- Adicionado override de `qs` em versão corrigida para runtime.

## Melhorias de Testes

- Testes HTTP com `supertest`.
- Cobertura de liveness, readiness, listagem, criação, alias legado, id inválido, JSON inválido e limite de requisições.
- Suíte atual: 11 testes passando.

## Melhorias de Container e DevOps

- Dockerfiles atualizados para Node 24.15.0.
- Healthcheck do backend aponta para `/readiness`.
- Compose recebeu variáveis de serviço, log e limite de requisições.
- GitHub Actions cobre formatação, lint, typecheck, testes, auditoria, varredura de segredo, SBOM, CodeQL, revisão de dependências, build OCI e Trivy scan.
- Dependabot configurado.

## Dívida Técnica Remanescente

- Migrações ainda não possuem histórico versionado.
- Sem teste de integração com MySQL por falta de Docker local.
- Vulnerabilidades moderadas dev-only permanecem no toolchain Angular atual.
- Sem autenticação/autorização por ausência de requisito multiusuário.

## Riscos

- Docker local não validado no host.
- Trivy/CodeQL rodam apenas no GitHub Actions, não localmente.
- Limite de requisições em memória não é adequado para múltiplas réplicas.

## Mudanças Incompatíveis

Nenhuma breaking change intencional. `/api/tasks` foi mantido e `/api/v1/tasks` foi adicionado.

## Instruções de Migração

Para consumidores novos, usar `/api/v1/tasks`. Consumidores existentes em `/api/tasks` continuam funcionando.

Para desenvolvimento local:

```bash
npm ci
npm run format:check
npm run lint
npm run typecheck
npm test
npm run build
```

Para Docker:

```bash
cp .env.example .env
docker compose up --build
```

## Próximos Passos Recomendados

- P0: instalar Docker local ou validar Compose em CI/host com runtime OCI.
- P1: adicionar teste de integração MySQL.
- P1: habilitar branch protection com checks obrigatórios.
- P1: definir registry, tags SemVer, assinatura e provenance.
- P2: monitorar Angular build tooling para eliminar vulnerabilidades dev-only.
- P2: adicionar histórico de migrações se o banco evoluir.
- P3: avaliar autenticação/autorização apenas se houver requisito multiusuário.
